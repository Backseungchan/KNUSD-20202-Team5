<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lcw.ex85firebasechatting">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrderListActivity" android:parentActivityName=".MyProfileActivity"></activity>
        <activity
            android:name=".MyProfileActivity"
            android:parentActivityName=".ChatActivity" />
        <activity android:name=".OrderSuccessActivity" />
        <activity
            android:name=".OrderActivity"
            android:parentActivityName=".YourProfileActivity" />
        <activity
            android:name=".YourProfileActivity"
            android:parentActivityName=".ChatActivity" />
        <activity
            android:name=".ChatActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>